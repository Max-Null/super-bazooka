# 变更记录

## [0.1.5] — 2026-07-01

### 提示词优化

- 输入框新增 ✨ 优化按钮——点击后调 LLM 改写模糊描述为清晰指令，结果填入输入框可手动编辑后发送
- 聊天 API 地址独立配置（CC 配置栏），用于优化/翻译/MCP 描述生成，留空自动跟随 Base URL
- 双击测试连接——同时测 CC 端点和聊天 API 端点，分两行独立显示（绿✓/红✕/琥珀⚠）
- 设置页 🔍 查询按钮——静默调 CC 联网搜索当前模型的 chat completions 端点并自动填入

### 一键安装 CC

- 嵌入 install-cc.ps1 脚本，设置中点击即可在新窗口完成 CC CLI 安装

### UI 可见性

- 深色/亮色主题 header 按钮和图标改用 accent/text-secondary 替代 text-muted
- 工作区切换按钮统一 accent-dim 绿色边框
- 附件/指令等工具栏按钮提亮
- 管理面板扳手图标改为 SVG 风格统一
- 消息操作图标（复制/编辑/重发/取消）提亮

### 管理面板增强

- 权限面板展示完整 allow/deny 规则列表
- Agents 和 Plugins 标签补「重新翻译」按钮
- Skills 加载多项版本缓存去重

### 其他

- 中途发送消息不再清空调试日志和计划状态
- 发送按钮纸飞机图标微调居中
- 输入框发送后自动恢复单行高度

---

## [0.1.4] — 2026-06-30

### 会话日志

- Debug / LLM Request 日志面板——绝对定位白色背景，点击面板外关闭，按钮组水平布局在输入框上方
- 日志按会话隔离并持久化到 SQLite `sessions` 表（`debug_log` / `stderr_log` 列），切换会话时恢复
- `createSessionLog(maxLines)` 工厂函数消除 useDebugLog / useStderrLog 95% 重复代码

### 消息与附件

- 附件文件路径写入 NDJSON 用户消息——CC 可感知附加文件
- 会话进行中发送新消息——利用 CC stdin 队列语义，不中断当前处理
- 编辑/重发消息不再删除原消息，附件自动恢复

### Token 与计时

- Token 累计修复——`message_delta` 事件提取 `usage` 数据，ContextIndicator 随会话实时更新
- 工具执行实时计时——`⚡Xs` 动画脉冲计数器，长操作（>5s）珊瑚色高亮
- 思考时间修正——`elapsedSeconds` 改用 `totalThinkingMs()` 而非总耗时

### 工作区管理

- 工作区切换——停止 CC → 清除 → 以新 CWD 创建会话（`path` 经 `canonicalize` + `is_dir` 校验）
- 最近工作区——localStorage 持久化最近 10 个路径，下拉菜单快速切换

### Ponytail 集成

- 设置页 Ponytail 插件检测——未安装显示安装按钮，已安装显示模式下拉
- 模式切换通过 CC 消息发送 `/ponytail` 命令

### 其他

- 文件目录刷新按钮、自动刷新
- 输入框滚动条、路径面包屑溢出修复
- 欢迎页键盘快捷键 i18n
- ANTHROPIC_AUTH_TOKEN / ANTHROPIC_API_KEY 双 key 兼容

---

## [0.1.3] — 2026-06-29

### Provider 配置

- 预设 Provider 的 Base URL 支持自定义——用户编辑的 baseUrl 优先于预设默认值，空则回退默认
- DeepSeek 默认 URL 统一 `/anthropic` 后缀
- Anthropic Provider 检测兜底——空 API Key 场景下仍正确识别为 Anthropic，防止切换时清理逻辑跳过
- Provider 切换全链路审计——确认 key 清理、detect_provider、base_url 写入均正确

### CLAUDE.md 精简

- cc-gui CLAUDE.md 从 367 行精简到 86 行（-76%）——只保留压缩后必须存活的骨架
- 31 条设计决策 + 版本号管理移入 `docs/知识/设计决策参考.md`
- 新增 `docs/知识/CLAUDE-MD精简整改方案.md`——可移植到其他 CC 项目的优化方法论
- 设计决策参考文档三层防线——CLAUDE.md 触发规则 + Memory 冗余指针 + Hook 不受影响

### Ponytail 插件检测

- 工具栏检测 Ponytail 插件是否安装——已安装显示下拉，未安装显示安装按钮
- 安装按钮 → 发送消息给 CC，由 CC 处理安装
- 三态 null/true/false 消除首次渲染闪烁

### 更新日志

- 设置页 About 区域新增"更新日志"入口——点击弹窗查看完整变更记录
- 变更记录编译期打包进应用（Vite `?raw` import），离线可用

---

## [0.1.2] — 2026-06-27

### 计划审核

- ExitPlanMode 计划审核弹窗 — Markdown 渲染计划内容，五个操作：执行计划（+ set_permission_mode acceptEdits 绕过 CC 无限循环 bug）、继续设计、停止计划、输出计划、反馈
- 计划关闭后可重新打开 — `📋 查看计划` 快捷入口，已审核计划仅保留导出功能

### UI 体验

- 删除当前会话后回到首页（清空消息 + 导航到 `/chat`）
- 手动停止思考后消息标记 `⏹ 已停止`（珊瑚色）
- 审批队列 — `pendingControlRequest` 改为队列，防止子 agent 并发审批互相覆盖
- 版本号统一从 `package.json` 注入 UI — Vite `define` 自动同步
- UI 样式速查表写入 CLAUDE.md — 6 类复用模式覆盖按钮/输入框/面板/状态/字号/动画

### 工程

- CHANGELOG 移入 `docs/变更记录.md`，遵循中文命名规范
- 发版版本号三处同步指南写入 CLAUDE.md

---

## [0.1.1] — 2026-06-27

### NDJSON 交互协议 (P0/P1)

- **P0-1**: CC 子进程启动参数修正 — 添加 `--input-format stream-json` 和 `--permission-prompt-tool stdio`，移除 `--print`，消息通过 stdin NDJSON 发送
- **P0-2**: 修正 `control_response` 格式为嵌套结构 `response.subtype/request_id/response.behavior/updatedInput/message`，新增 `request_id` 往返和 `set_permission_mode` 初始化
- **P1-1**: `hook_callback` 不再自动允许，转发前端走用户审批流程
- **P1-2**: `AskUserQuestion` 问答弹窗 — radio/checkbox 选项组 + Other 自由输入，答案格式 `{questions, answers}`
- **P1-3**: interrupt 优雅停止 — 先发 `control_request(subtype: "interrupt")`，3 秒超时兜底
- **P1-4**: deny 含 `message`、allow 含 `updatedInput`（含在 P0-2）

### i18n 修复

- 修复 vue-i18n v10 消息编译器 `SyntaxError: 10` — `@` 用 `{'@...'}` 转义，`{ }` 字面量修正
- 授权弹窗 "Allow Bash?" 国际化 — `chat.allowTool: "允许使用 {tool}？"`
- 工具名翻译 — `Bash→命令行`、`Write→写入文件`、`Read→读取文件` 等，中英双份

### UI 改进

- 思考计时重构 — 每段 tool_use 前独立计时（`🧠Xs`），总耗时 = 各段求和，去除实时计时器，不受审批暂停影响
- Debug 面板复制按钮 — 与消息气泡同款 SVG 图标，鼠标移入显示
- 问答弹窗样式修复 — header 标签 `whitespace-nowrap`、去掉双滚动条

### 工程

- 版本号管理 — 从 `package.json` 注入 `__APP_VERSION__` 到 UI，三处同步（`package.json` / `tauri.conf.json` / `Cargo.toml`）
- 安全审查修复 — `hook_callback` 从自动允许改回用户审批

---

## [0.1.0] — 2026-06

### 核心功能

- Tauri 2 + Vue 3 + TypeScript 桌面应用架构
- Claude Code CLI 子进程管理 — 三线程模型（Waiter / Stdout Reader / Stderr）
- NDJSON stream-json 协议解析 — `StreamLine` + `StreamFrontendEvent`
- 会话管理 — 创建/删除/重命名/切换，SQLite 持久化
- 聊天界面 — 流式消息渲染、Markdown + Mermaid 渲染、审批栏
- 文件浏览 — FilePanel / FileTree / FilePreview / DiffViewer
- 命令面板 — 拼音搜索 + 动态命令注册
- 8 合 1 管理面板 — plugins/mcp/skills/agents/hooks/memory/permissions/styles
- API 设置 — DeepSeek API 配置 + 连接测试
- 国际化 — vue-i18n 中英双语，所有面向用户文本双份
- 主题 — 暗色/亮色/系统自动

### 权限系统

- 6 种权限模式 — `default` / `acceptEdits` / `bypassPermissions` / `plan` / `dontAsk` / `auto`
- `sync_permission_settings()` 写入 `~/.claude/settings.json`
- 批准场景 CRUD（SQLite `approved_scenarios` 表）

### 工程

- vitest 单元测试 + Playwright E2E + Rust 集成测试
- Conventional Commits 提交规范
- CLAUDE.md 项目文档 + docs/ 中文文档体系
