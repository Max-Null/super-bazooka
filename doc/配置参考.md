# 配置参考

> 本文档由 doc-generator 自动生成，最后一次扫描：2026-06-26
> 共覆盖 18 个模块、7 个配置项

## 前端-设置

### `env.*`

| 配置键 | 类型 | 默认值 | 必填 | 说明 |
|--------|------|--------|------|------|
| `env.VITE_DEV_SERVER_URL` | `string` | - | 否 | Vite 开发服务器地址（开发模式由 Vite 自动注入） |

## 前端-Store

### `localStorage.*`

| 配置键 | 类型 | 默认值 | 必填 | 说明 |
|--------|------|--------|------|------|
| `localStorage.cc-gui-ui-settings` | `object` | `{ planMode: false, autoMode: true, permissionMode: "bypassPermissions", effort: "high", ponytailMode: "full", theme: "dark", locale: "zh", fontSize: "medium", claudePath: "" }` | 否 | UI 偏好持久化键 |

## Rust-Tauri命令

### `env.*`

| 配置键 | 类型 | 默认值 | 必填 | 说明 |
|--------|------|--------|------|------|
| `env.CLAUDE_CODE_SUBAGENT_MODEL` | `string` | `deepseek-chat` | 否 | 子代理/翻译用模型（通过 `~/.claude/settings.json` 的 env 字段配置） |

## Rust-数据库

### `db.*`

| 配置键 | 类型 | 默认值 | 必填 | 说明 |
|--------|------|--------|------|------|
| `db.journal_mode` | `string` | `WAL` | 否 | SQLite journal 模式（PRAGMA journal_mode=WAL） |
| `db.foreign_keys` | `string` | `ON` | 否 | 外键约束（PRAGMA foreign_keys=ON） |

### Schema 表清单

| 表名 | 字段数 | 说明 |
|------|--------|------|
| sessions | 8 | 会话表：id / title / cli_session_id / cwd / model / status / created_at / updated_at |
| messages | 6 | 消息表：id / session_id(FK CASCADE) / role(CHECK) / content(JSON) / token_usage / created_at |
| settings | 2 | 设置表：key / value |
| approved_scenarios | 3 | 审批场景表：tool_name / pattern(PK) / created_at |
| item_descriptions | 5 | 翻译缓存表：item_type / name(PK) / desc_en / desc_zh / translated_at |

## 附录：`~/.claude/settings.json` 关键字段

以下字段由 cc-gui 自动管理，用户通常不需要手动编辑：

| 配置键 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| `env.ANTHROPIC_AUTH_TOKEN` | `string` | - | DeepSeek API Key |
| `env.ANTHROPIC_BASE_URL` | `string` | `https://api.deepseek.com` | API 端点（Anthropic 兼容格式） |
| `env.ANTHROPIC_MODEL` | `string` | `deepseek-v4-pro[1M]` | 默认模型 |
| `effortLevel` | `string` | `high` | 思考深度：low / medium / high / xhigh / max |
| `permissions.defaultMode` | `string` | `default` | 权限模式：default / acceptEdits / bypassPermissions / plan / auto / dontAsk |

<!-- @generated v0.5.1 -->
<!-- @baseline commit=f67115370991f3521ab8aece00f990d651886eac generated=2026-06-26T12:00:00+08:00 -->
